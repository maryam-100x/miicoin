{
  "version": 2,
  "builds": [
    { "src": "api/generate-mii.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/generate-mii", "dest": "/api/generate-mii.js" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
